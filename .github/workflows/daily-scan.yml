name: Daily AI & Cyber Scan
on:
  schedule:
    - cron: '0 4 * * *'    # 04:00 UTC = 07:00 Israel
  workflow_dispatch:         # Manual trigger

concurrency:
  group: daily-scan
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Scan RSS feeds
        run: python scripts/scan.py

      - name: Deduplicate articles
        run: python scripts/deduplicate.py

      - name: Summarize with Groq
        run: python scripts/summarize.py
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}

      - name: Send email report
        run: python scripts/send_email.py
        env:
          EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}

      - name: Show summarize debug info
        if: always()
        run: cat data/summarize_debug.json 2>/dev/null || echo "No debug file found"

      - name: Copy data to docs for GitHub Pages
        run: |
          mkdir -p docs/data/articles
          cp data/articles/*.json docs/data/articles/

      - name: Commit and push data
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add data/ docs/
          git commit -m "Daily scan $(date +%Y-%m-%d)" || exit 0
          git push
